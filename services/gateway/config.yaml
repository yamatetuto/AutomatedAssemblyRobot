# Gateway Service Configuration

service:
  name: gateway
  version: "1.0.0"
  host: "0.0.0.0"
  port: 8000

# バックエンドサービスのURL
services:
  camera:
    url: "http://localhost:8001"
    prefix: "/api/camera"
  gripper:
    url: "http://localhost:8002"
    prefix: "/api/gripper"

logging:
  level: "INFO"
  directory: "../../logs"
