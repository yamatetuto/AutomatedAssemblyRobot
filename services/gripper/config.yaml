# Gripper Service Configuration

service:
  name: gripper
  version: "1.0.0"
  host: "0.0.0.0"
  port: 8002

gripper:
  port: "/dev/ttyUSB0"
  slave_address: 1
  baudrate: 38400
  timeout: 1.0

logging:
  level: "INFO"
  directory: "../../logs"
