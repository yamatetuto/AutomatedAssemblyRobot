# =============================================================================
# AutomatedAssemblyRobot 依存パッケージ
# =============================================================================
# 
# ■ インストール手順
# 
# 1. aptでインストール可能なパッケージを先にインストール:
#    sudo apt install -y python3-opencv python3-numpy python3-aiohttp \
#        python3-websockets python3-jinja2 python3-yaml python3-dotenv \
#        python3-serial python3-spidev python3-smbus python3-rpi.gpio \
#        python3-fastapi python3-uvicorn python3-pydantic python3-av \
#        python3-multipart python3-pytest python3-flake8 python3-mypy
#
# 2. aptでインストールできないパッケージをpipでインストール:
#    pip3 install --break-system-packages aiortc minimalmodbus pytest-asyncio pytest-cov
#
# =============================================================================

# ----- pip専用パッケージ (aptで利用不可) -----

# WebRTC (カメラストリーミング)
# aiortc>=1.6.0
# Python3.9.x
aiortc>=1.5.0
# NOTE (Raspberry Pi): aiortcはPyAV(av)に依存。
# 1) 既存のapt版python3-avを使う場合は、venvを --system-site-packages で作成し
#    aiortcは --no-deps でインストールする。
# 2) pipでPyAVをビルドする場合は、FFmpeg開発ヘッダを先に入れ、
#    互換のため av==9.2.0 などに固定する。
# av==9.2.0
# HTTPクライアント（カメラPiプロキシ）
# aiohttp>=3.8.0
# Python3.9.x
aiohttp>=3.7.4


# Modbus通信 (グリッパー制御)
minimalmodbus>=2.1.1

# テスト追加パッケージ
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0

# ----- 以下はaptでインストール推奨 (参考情報) -----
# 
# ■ カメラ・映像処理
#   apt: python3-opencv python3-numpy python3-aiohttp python3-av
#
# ■ ハードウェア通信
#   apt: python3-serial python3-spidev python3-smbus python3-rpi.gpio
#
# ■ Webアプリケーション
#   apt: python3-fastapi python3-uvicorn python3-pydantic 
#        python3-multipart python3-jinja2 python3-websockets
#
# ■ 設定管理
#   apt: python3-yaml python3-dotenv
#
# ■ テスト・開発ツール
#   apt: python3-pytest python3-flake8 python3-mypy
